{
  "name": "lokyy-updater",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Update sidecar for lokyy-brain. Deliberately NOT a pnpm workspace member: it holds the Docker socket and must not share a dependency tree with the application it updates.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0",
    "vitest": "^1.6.0"
  }
}
